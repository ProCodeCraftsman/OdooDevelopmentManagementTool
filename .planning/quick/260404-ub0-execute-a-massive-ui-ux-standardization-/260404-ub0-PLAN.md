---
phase: ub0-massive-ui-ux-standardization
plan: "01"
type: execute
wave: "1"
depends_on: []
files_modified:
  - frontend/src/components/ui/dropdown-menu.tsx
  - frontend/src/components/ui/table.tsx
  - frontend/src/pages/modules.tsx
  - frontend/src/pages/environments/details.tsx
autonomous: true
must_haves:
  truths:
    - "Dropdowns are fully opaque and legible in both light and dark modes"
    - "Table headers have strict visual boundary, content scrolls behind headers"
    - "Module Master page uses Universal Table Pattern with first_seen_date column"
    - "Environment Details page shows module table with versions and dependencies"
  artifacts:
    - path: "frontend/src/components/ui/dropdown-menu.tsx"
      provides: "Opaque dropdown menus"
      contains: "bg-popover.*solid|bg-background.*solid"
    - path: "frontend/src/components/ui/table.tsx"
      provides: "Fixed table header positioning"
      contains: "sticky.*z-10|position.*sticky"
    - path: "frontend/src/pages/modules.tsx"
      provides: "Universal Table on Module Master"
      contains: "DataTable|first_seen_date"
    - path: "frontend/src/pages/environments/details.tsx"
      provides: "Module table on Environment Details"
      contains: "DataTable|module_name|installed_version"
---

<objective>
Execute a massive UI/UX standardization and bug-fix pass across the frontend. Fix dropdown/popover visibility issues, table header overlap, and implement Universal Table Pattern on Module Master and Environment Details pages.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@AGENTS.md (project conventions, API contract specs)
@.planning/quick/260404-ub0-execute-a-massive-ui-ux-standardization-/260404-ub0-CONTEXT.md

## Key Requirements from CONTEXT.md

**Priority UI Fixes:**
- Dropdown/Popover visibility: fully opaque, legible in both modes
- Table header overlap: strict visual boundary, scrolling content behind headers

**Universal Table Pattern:**
- Server-side pagination: 15 records/page default
- Column sorting: Shadcn table sorting UI indicators
- Advanced filtering: Multi-column filter inputs
- Group-by views: Toggle for grouping by primary categories

**Module Master Page:**
- Display first_seen_date column
- Technical Name filter
- Use DataTable component

**Environment Details Page:**
- Add new table beneath summary cards
- Module Details, Installed Versions, Dependency Versions
- Use DataTable component (15 records/page, sorting, filters, group-by)
</context>

<interfaces>
<!-- Key hooks that exist for these pages -->

From frontend/src/hooks/useModuleMaster.ts:
```typescript
// Note: This hook may need updating to support pagination params
export function useModuleMaster(params: ModuleMasterParams): UseQueryResult<ModuleMasterResponse>
```

From frontend/src/hooks/useEnvironmentModules.ts:
```typescript
export function useEnvironmentModules(envName: string, params: EnvironmentModulesParams): UseQueryResult<EnvironmentModulesResponse>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix Dropdown/Popover visibility and Table header overlap</name>
  <files>frontend/src/components/ui/dropdown-menu.tsx, frontend/src/components/ui/table.tsx</files>
  <action>
    **DropdownMenuContent fix (per D-01 priority):**
    - Update bg-popover class to ensure full opacity: add `bg-popover` with explicit solid background in tailwind config or inline style
    - Ensure text is legible: verify text-popover-foreground provides sufficient contrast
    - Add `bg-background` fallback for cases where popover doesn't apply

    **Table header fix (per D-01 priority):**
    - In table.tsx: Add `sticky top-0 z-10` to TableHeader for proper stacking
    - Ensure TableHead has `bg-background` or `bg-muted/50` to create visual boundary
    - Verify content scrolls behind headers (not overlapping)
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | head -30</automated>
  </verify>
  <done>Dropdown menus render with solid backgrounds in both light/dark modes; Table headers stay fixed while content scrolls</done>
</task>

<task type="auto">
  <name>Task 2: Implement Universal Table Pattern on Module Master and Environment Details</name>
  <files>frontend/src/pages/modules.tsx, frontend/src/pages/environments/details.tsx</files>
  <action>
    **Module Master page (modules.tsx):**
    - Import DataTable, DataTableColumnHeader from @/components/ui/data-table
    - Replace custom HTML table with DataTable component
    - Add first_seen_date column (format: new Date(row.first_seen_date).toLocaleDateString())
    - Add technical_name as filterable column
    - Use default 15 records/page pagination
    - Add sorting on technical_name and first_seen_date columns
    - Keep search placeholder: "Filter by technical name..."
    
    **Environment Details page (details.tsx):**
    - Import DataTable, DataTableColumnHeader from @/components/ui/data-table
    - Create new module data table with columns: technical_name, module_name, installed_version, dependency_versions (JSON stringify for display), state
    - Add beneath the existing summary cards (after the grid div)
    - Use default 15 records/page pagination
    - Add sorting on all columns
    - Add state as filterable column
    - Add group-by toggle for state column
    
    **Note:** Build frontend ready for API even if backend not implemented yet - use local state or mock data to demonstrate pattern
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | head -30</automated>
  </verify>
  <done>Module Master page displays DataTable with first_seen_date; Environment Details shows module table with version columns</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` completes without errors
- [ ] Dropdown menus render with full opacity in both light and dark themes
- [ ] Table headers remain fixed during scroll (content behind headers, not overlapping)
- [ ] Module Master page shows DataTable with sorting, filtering, pagination
- [ ] Module Master shows first_seen_date column
- [ ] Environment Details page shows module table with version information
</verification>

<success_criteria>
All tables in the application use Universal Table Pattern (DataTable component with pagination, sorting, filtering, group-by). UI bugs fixed: dropdowns fully opaque, table headers properly stacked.
</success_criteria>

<output>
After completion, create `.planning/quick/260404-ub0-execute-a-massive-ui-ux-standardization-/260404-ub0-01-SUMMARY.md`
</output>
