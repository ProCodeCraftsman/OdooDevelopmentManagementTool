---
phase: 12-group-by-dynamic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/repositories/request_module_line.py
  - frontend/src/api/development-requests.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - Backend API accepts new group_by values for request attributes
    - Frontend API types support new group_by filter options
  artifacts:
    - path: backend/app/repositories/request_module_line.py
      provides: Extended group_by support for request fields
    - path: frontend/src/api/development-requests.ts
      provides: Extended DevelopmentRequestLineFilters type
  key_links:
    - from: backend/app/repositories/request_module_line.py
      to: backend/app/api/v1/development_requests.py
      via: DevelopmentRequestService.get_module_lines()
---

<objective>
Extend backend and frontend API to support grouping DR lines by request attributes (Type, State, Category, Priority, Assignee).

Purpose: Enable the grouping feature foundation before UI implementation
Output: Backend and frontend API ready for grouping
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-group-by-dynamic/12-CONTEXT.md
@backend/app/repositories/request_module_line.py
@frontend/src/api/development-requests.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend backend group_by support</name>
  <files>backend/app/repositories/request_module_line.py</files>
  <behavior>
    - Test 1: get_group_counts("request_type") returns grouped counts by request type name
    - Test 2: get_group_counts("request_state") returns grouped counts by request state name
    - Test 3: get_group_counts("functional_category") returns grouped counts by category name
    - Test 4: get_group_counts("priority") returns grouped counts by priority level (sorted)
    - Test 5: get_group_counts("assigned_developer") returns grouped counts with "Unassigned" for null
  </behavior>
  <action>
1. Import required models: RequestType, RequestState, FunctionalCategory, Priority, User
2. Extend get_group_counts() to handle:
   - "request_type": JOIN with DevelopmentRequest, group by RequestType.name
   - "request_state": JOIN with DevelopmentRequest, group by RequestState.name (with category)
   - "functional_category": JOIN with DevelopmentRequest, group by FunctionalCategory.name
   - "priority": JOIN with DevelopmentRequest, group by Priority.level then Priority.name
   - "assigned_developer": JOIN with DevelopmentRequest, group by User.username with COALESCE for null (label "Unassigned")
3. Ensure each returns: {key, label, count} format
4. Update _apply_filters to support the JOINs needed for these group_by options
  </action>
  <verify>
    <automated>pytest backend/tests/test_repositories/test_request_module_line.py -v -k "group" 2>/dev/null || echo "No tests - verify manually via API call"</automated>
  </verify>
  <done>Backend accepts group_by=request_type|request_state|functional_category|priority|assigned_developer and returns proper group counts</done>
</task>

<task type="auto">
  <name>Task 2: Update frontend API types</name>
  <files>frontend/src/api/development-requests.ts</files>
  <action>
1. Update DevelopmentRequestLineFilters interface:
   - Change group_by type from `"module" | "uat_status"` to:
     ```typescript
     group_by?: "module" | "uat_status" | "request_type" | "request_state" | "functional_category" | "priority" | "assigned_developer";
     ```
2. Verify the types match what the API will return (should be auto-serialized from backend GroupInfo)
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Frontend API types support all new group_by values</done>
</task>

</tasks>

<verification>
- Backend test: Call `/api/v1/development-requests/lines/all?group_by=request_type` returns groups
- Backend test: Call `/api/v1/development-requests/lines/all?group_by=assigned_developer` includes "Unassigned" group
- Frontend: TypeScript compiles without errors
</verification>

<success_criteria>
Backend API accepts all 7 group_by values and returns properly formatted group counts. Frontend types are updated to match.
</success_criteria>

<output>
After completion, create `.planning/phases/12-group-by-dynamic/12-01-SUMMARY.md`
</output>