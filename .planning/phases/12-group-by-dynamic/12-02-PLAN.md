---
phase: 12-group-by-dynamic
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - frontend/src/components/development-requests/dr-group-header.tsx
  - frontend/src/pages/development-requests/lines-list.tsx
  - frontend/src/hooks/useDevelopmentRequests.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - User can select group by attribute via dropdown
    - Table displays grouped rows with collapsible headers
    - Group headers show label and item count
    - User can expand/collapse individual groups
    - User can expand/collapse all groups
  artifacts:
    - path: frontend/src/components/development-requests/dr-group-header.tsx
      provides: Group header component with expand/collapse
    - path: frontend/src/pages/development-requests/lines-list.tsx
      provides: Main view with grouping state and rendering
  key_links:
    - from: frontend/src/pages/development-requests/lines-list.tsx
      to: frontend/src/components/development-requests/dr-group-header.tsx
      via: Import and render grouped rows
---

<objective>
Implement dynamic group-by UI for DR Module Lines view.

Purpose: Enable users to organize lines by Type, State, Category, Priority, or Assignee with collapsible OpenProject-style headers
Output: Working group-by UI with expand/collapse functionality
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-group-by-dynamic/12-CONTEXT.md
@frontend/src/pages/development-requests/lines-list.tsx
@frontend/src/api/development-requests.ts
@.opencode/phases/12-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GroupHeader component</name>
  <files>frontend/src/components/development-requests/dr-group-header.tsx</files>
  <action>
Create new component `DrGroupHeader`:

1. Props:
   - `label: string` - Group label (e.g., "Bug", "In Progress")
   - `count: number` - Number of items in group
   - `isExpanded: boolean` - Current expand state
   - `onToggle: () => void` - Toggle callback
   - `className?: string` - Optional styling

2. Implementation:
   - Use ChevronRight/ChevronDown from lucide-react for expand indicator
   - Click anywhere on header toggles (button with w-full)
   - Show count badge: "{count} item(s)"
   - Use smooth transitions for expand/collapse (CSS transition on max-height or display)
   - Apply styling per UI-SPEC.md: bg-secondary, font-semibold heading 16px, hover:bg-secondary/80

3. Export: Named export `DrGroupHeader`
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>DrGroupHeader component exists with proper props and styling</done>
</task>

<task type="auto">
  <name>Task 2: Add grouping state to lines-list page</name>
  <files>frontend/src/pages/development-requests/lines-list.tsx</files>
  <action>
1. Add group_by state:
   ```typescript
   const [groupBy, setGroupBy] = useState<DevelopmentRequestLineFilters["group_by"]>(undefined);
   ```

2. Add expandedGroups state:
   ```typescript
   const [expandedGroups, setExpandedGroups] = useState<Set<string>>(new Set());
   ```
   - Initialize all groups as expanded

3. Add group options dropdown in header area (after query bar, before table):
   - Label: "Group by:" with Select dropdown
   - Options: None (default), Type, State, Category, Priority, Assignee
   - Use shadcn Select component

4. Add expand/collapse all controls (visible only when groupBy is set):
   - "Expand All" / "Collapse All" buttons

5. Update API filters to include group_by:
   ```typescript
   const apiFilters = useMemo<DevelopmentRequestLineFilters>(() => ({
     module_names: filters.module_names,
     uat_statuses: filters.uat_statuses,
     search: filters.search || undefined,
     group_by: groupBy || undefined,
   }), [filters, groupBy]);
   ```

6. Initialize expandedGroups when groups change (useEffect on data.groups)
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>lines-list page has group_by state, dropdown, and expand/collapse controls</done>
</task>

<task type="auto">
  <name>Task 3: Render grouped rows</name>
  <files>frontend/src/pages/development-requests/lines-list.tsx</files>
  <action>
1. Modify table rendering logic to handle grouped vs flat display:

   When groupBy is active and data.groups exists:
   - For each group in data.groups:
     - Render DrGroupHeader with label, count, isExpanded, onToggle
     - If group is expanded, render all rows belonging to that group
     - Group rows by extracting the grouping key from each item

2. Maintain flat rendering when groupBy is undefined (existing behavior)

3. For group key extraction:
   - request_type: item.request?.request_type?.name
   - request_state: item.request?.request_state?.name
   - functional_category: item.request?.functional_category?.name
   - priority: item.request?.priority?.name
   - assigned_developer: item.request?.assigned_developer?.username ?? "Unassigned"
   - module: item.module_technical_name
   - uat_status: item.uat_status ?? "None"

4. Handle edge cases:
   - Groups with 0 items: don't render (or show collapsed)
   - Group key not found on item: render under "Unknown" group

5. Store expanded state in localStorage:
   - Key: "dr-lines-group-state"
   - Value: JSON of expanded group keys per groupBy type
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Table displays grouped rows with collapsible headers and state persistence</done>
</task>

</tasks>

<verification>
- Select "Type" from group dropdown → table shows groups by request type
- Click expand/collapse on a group → rows show/hide with animation
- Click "Expand All" → all groups expand
- Refresh page → expanded state persists (from localStorage)
</verification>

<success_criteria>
User can group DR lines by Type, State, Category, Priority, or Assignee with collapsible OpenProject-style headers. Expand/collapse all works. State persists across sessions.
</success_criteria>

<output>
After completion, create `.planning/phases/12-group-by-dynamic/12-02-SUMMARY.md`
</output>