---
phase: 11-settings-control-parameters
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/v1/development_requests.py
  - backend/app/schemas/control_parameters.py
  - frontend/src/api/control-parameters.ts
autonomous: true
requirements:
  - "Backend: Add PATCH endpoint for updating control parameters"
  - "Backend: Control Parameter Rules model and API"

must_haves:
  truths:
    - "PATCH /control-parameters/{param_type}/{id} endpoint exists"
    - "Update endpoint allows updating name and description fields"
    - "Category, type, priority, level fields cannot be modified via API"
    - "ControlParameterRule model exists in database"
    - "API endpoint exists to list/update rules"
  artifacts:
    - path: "backend/app/api/v1/development_requests.py"
      provides: "PATCH endpoint for control parameter updates"
      contains: "@router.patch"
    - path: "backend/app/schemas/control_parameters.py"
      provides: "Update schema for control parameters"
      contains: "ControlParameterUpdate"
    - path: "frontend/src/api/control-parameters.ts"
      provides: "update function for frontend API client"
      contains: "update:"
  key_links:
    - from: "frontend/src/hooks/useControlParameters.ts"
      to: "backend/app/api/v1/development_requests.py"
      via: "PATCH request"
      pattern: "control-parameters.*PATCH"
---

<objective>
Add backend support for updating control parameters (name and description only) and create the Control Parameter Rules feature for configurable request state transitions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/app/api/v1/development_requests.py
@backend/app/schemas/control_parameters.py
@backend/app/models/control_parameters/request_type.py
@backend/app/models/control_parameters/request_state.py
@.planning/phases/11-settings-control-parameters/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PATCH endpoint for updating control parameters</name>
  <files>backend/app/api/v1/development_requests.py, backend/app/schemas/control_parameters.py</files>
  <action>
    Add PATCH endpoint to allow updating control parameters (name and description only):
    1. In schemas/control_parameters.py, add ControlParameterUpdate schema with name and description fields
    2. In development_requests.py, add PATCH endpoint at /control-parameters/{param_type}/{id}
    3. The endpoint should:
       - Accept param_type (request-types, request-states, priorities, functional-categories)
       - Accept id and update data
       - Allow updating name and description fields
       - Reject attempts to update category, type, level with 400 error
       - Require admin authentication (get_current_admin_user)
    4. Add response schema (RequestTypeResponse, etc.)
  </action>
  <verify>
    <automated>grep -n "router.patch\|@router.patch" backend/app/api/v1/development_requests.py</automated>
  </verify>
  <done>PATCH endpoint exists, allows name and description update, rejects category/level changes</done>
</task>

<task type="auto">
  <name>Task 2: Add update function to frontend API client</name>
  <files>frontend/src/api/control-parameters.ts</files>
  <action>
    Add update function to the frontend API client:
    1. Add ControlParameterUpdate interface
    2. Add update method to controlParametersApi object
    3. Make PATCH request to /development-requests/control-parameters/{paramType}/{id}
  </action>
  <verify>
    <automated>grep -n "update:" frontend/src/api/control-parameters.ts</automated>
  </verify>
  <done>update function available in controlParametersApi</done>
</task>

<task type="auto">
  <name>Task 3: Create ControlParameterRule model</name>
  <files>backend/app/models/control_parameter_rule.py</files>
  <action>
    Create the ControlParameterRule model:
    1. Create new model file: backend/app/models/control_parameter_rule.py
    2. Model fields:
       - id (primary key)
       - request_state_name (String) - the request state this rule applies to
       - allowed_type_categories (String) - comma-separated: "ALL" or "Development,Non Development"
       - allowed_priorities (String) - comma-separated: "ALL" or "1,2,3,4,5"
       - allowed_functional_categories (String) - comma-separated: "ALL" or list of categories
       - is_active (Boolean, default=True)
       - created_at, updated_at timestamps
    3. Register in backend/app/models/__init__.py
    4. Create basic repository: backend/app/repositories/control_parameter_rule.py
  </action>
  <verify>
    <automated>grep -n "ControlParameterRule" backend/app/models/__init__.py</automated>
  </verify>
  <done>ControlParameterRule model exists and registered</done>
</task>

<task type="auto">
  <name>Task 4: Add Control Parameter Rules API endpoints</name>
  <files>backend/app/api/v1/development_requests.py</files>
  <action>
    Add API endpoints for managing control parameter rules:
    1. GET /control-parameters/rules - list all rules
    2. POST /control-parameters/rules - create new rule
    3. PUT /control-parameters/rules/{id} - update rule
    4. DELETE /control-parameters/rules/{id} - delete rule
    5. POST /control-parameters/rules/{id}/toggle - enable/disable rule
    6. Seed default rules based on the matrix from CONTEXT.md on first run
  </action>
  <verify>
    <automated>grep -n "rules" backend/app/api/v1/development_requests.py | head -15</automated>
  </verify>
  <done>CRUD endpoints for rules exist, default rules seeded</done>
</task>

<task type="auto">
  <name>Task 5: Add rule validation to DevelopmentRequestService</name>
  <files>backend/app/services/development_request_service.py</files>
  <action>
    Integrate rule validation into the service layer:
    1. Add method to validate request against control parameter rules
    2. Call validation during create and update operations
    3. Check if rules are enabled (admin configurable)
    4. If rules disabled, skip validation
    5. Return clear error message when rule is violated
  </action>
  <verify>
    <automated>grep -n "validate.*rule\|control_parameter_rule" backend/app/services/development_request_service.py</automated>
  </verify>
  <done>Service validates requests against control parameter rules</done>
</task>

</tasks>

<verification>
- [ ] PATCH endpoint works for updating name and description
- [ ] Attempting to update category/level returns 400 error
- [ ] Frontend update function calls correct API endpoint
- [ ] ControlParameterRule model created and migrated
- [ ] Rules API endpoints functional
- [ ] Default rules seeded on first run
- [ ] Service validates requests against rules
</verification>

<success_criteria>
Backend supports updating control parameters with field restrictions (name + description editable, category/level read-only), Control Parameter Rules feature is functional with seeded default rules.
</success_criteria>

<output>
After completion, create `.planning/phases/11-settings-control-parameters/11-02-SUMMARY.md`
</output>
