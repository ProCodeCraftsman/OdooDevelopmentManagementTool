---
phase: 11-settings-control-parameters
plan: 03
type: execute
wave: 2
depends_on: [11-02]
files_modified:
  - frontend/src/pages/settings/control-parameters.tsx
  - frontend/src/api/control-parameters.ts
  - frontend/src/hooks/useControlParameters.ts
autonomous: true
requirements:
  - "Admin UI: Manage Control Parameter Rules"

must_haves:
  truths:
    - "Rules tab exists in control parameters page"
    - "Admin can view list of all rules"
    - "Admin can create new rules"
    - "Admin can edit existing rules"
    - "Admin can toggle rules on/off"
    - "Admin can delete rules"
  artifacts:
    - path: "frontend/src/pages/settings/control-parameters.tsx"
      provides: "Rules management UI with tabs"
      contains: "RulesTab, rule edit sheet"
    - path: "frontend/src/api/control-parameters.ts"
      provides: "API functions for rules CRUD"
      contains: "rulesList, rulesCreate, rulesUpdate, rulesDelete"
  key_links:
    - from: "frontend/src/pages/settings/control-parameters.tsx"
      to: "frontend/src/api/control-parameters.ts"
      via: "rules API calls"
      pattern: "rulesList\|rulesCreate"
---

<objective>
Add Admin UI for managing Control Parameter Rules - allowing administrators to view, create, edit, toggle, and delete rules from the settings page.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/pages/settings/control-parameters.tsx
@frontend/src/api/control-parameters.ts
@.planning/phases/11-settings-control-parameters/11-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rules API functions to frontend</name>
  <files>frontend/src/api/control-parameters.ts</files>
  <action>
    Add rules management API functions:
    1. Add interfaces: ControlParameterRule, ControlParameterRuleCreate, ControlParameterRuleUpdate
    2. Add functions:
       - listRules(): GET /control-parameters/rules
       - createRule(data): POST /control-parameters/rules
       - updateRule(id, data): PUT /control-parameters/rules/{id}
       - deleteRule(id): DELETE /control-parameters/rules/{id}
       - toggleRule(id): POST /control-parameters/rules/{id}/toggle
  </action>
  <verify>
    <automated>grep -n "rules" frontend/src/api/control-parameters.ts | head -15</automated>
  </verify>
  <done>Rules API functions available in control-parameters.ts</done>
</task>

<task type="auto">
  <name>Task 2: Add rules hooks to useControlParameters</name>
  <files>frontend/src/hooks/useControlParameters.ts</files>
  <action>
    Add hooks for rules management:
    1. Add useControlParameterRules() - query hook for listing rules
    2. Add useCreateControlParameterRule() - mutation for creating rules
    3. Add useUpdateControlParameterRule() - mutation for updating rules
    4. Add useDeleteControlParameterRule() - mutation for deleting rules
    5. Add useToggleControlParameterRule() - mutation for toggling rules
  </action>
  <verify>
    <automated>grep -n "useControlParameterRule" frontend/src/hooks/useControlParameters.ts</automated>
  </verify>
  <done>Rules hooks available in useControlParameters.ts</done>
</task>

<task type="auto">
  <name>Task 3: Add Rules tab to control parameters page</name>
  <files>frontend/src/pages/settings/control-parameters.tsx</files>
  <action>
    Add Rules management tab to the control parameters page:
    1. Add "rules" to TAB_CONFIGS array or create separate RulesTab
    2. Create RulesTab component that displays rules in a table
    3. Table columns: State, Allowed Types, Allowed Priorities, Allowed Categories, Status, Actions
    4. Add Add button to create new rule
    5. Add edit button to modify existing rule
    6. Add toggle button to enable/disable rule
    7. Add delete button to remove rule
    8. Create edit Sheet for rule with fields:
       - request_state_name (dropdown of available states)
       - allowed_type_categories (multi-select: Development, Non Development, ALL)
       - allowed_priorities (multi-select: 1-5, ALL)
       - allowed_functional_categories (multi-select: all functional categories, ALL)
  </action>
  <verify>
    <automated>grep -n "RulesTab\|rules" frontend/src/pages/settings/control-parameters.tsx | head -15</automated>
  </verify>
  <done>Rules tab visible in control parameters page with full CRUD functionality</done>
</task>

</tasks>

<verification>
- [ ] Rules tab appears in control parameters page
- [ ] Rules table displays all rules with proper columns
- [ ] Can create new rule via sheet form
- [ ] Can edit existing rule
- [ ] Can toggle rule enabled/disabled
- [ ] Can delete rule
</verification>

<success_criteria>
Admin can fully manage Control Parameter Rules from the UI - create, read, update, toggle, and delete operations all functional.
</success_criteria>

<output>
After completion, create `.planning/phases/11-settings-control-parameters/11-03-SUMMARY.md`
</output>
