---
phase: 11-settings-control-parameters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/settings/control-parameters.tsx
autonomous: true
requirements:
  - "UI Enhancement: Rename Usage column to Count"
  - "UI Enhancement: Add edit functionality (name + description only)"

must_haves:
  truths:
    - "Usage column renamed to Count in control parameter tables"
    - "Edit button appears for each parameter row"
    - "Edit sheet allows editing name and description"
    - "Category, type, priority and other fields are read-only in edit form"
  artifacts:
    - path: "frontend/src/pages/settings/control-parameters.tsx"
      provides: "Control parameters table with edit functionality"
      contains: "ParameterRow, TableContent, edit sheet"
  key_links:
    - from: "frontend/src/pages/settings/control-parameters.tsx"
      to: "frontend/src/api/control-parameters.ts"
      via: "edit function call"
      pattern: "editMutation"
---

<objective>
Rename the "Usage" column to "Count" and add edit functionality that allows editing name and description (category, type, priority fields are read-only).
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/pages/settings/control-parameters.tsx
@frontend/src/api/control-parameters.ts
@.planning/phases/11-settings-control-parameters/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Usage column to Count in control parameter tables</name>
  <files>frontend/src/pages/settings/control-parameters.tsx</files>
  <action>
    In control-parameters.tsx, rename the "Usage" column to "Count":
    1. Change the TableHead text from "Usage" to "Count" (line 515)
    2. Keep the usage_count display in ParameterRow component but rename the column header
    3. The count should show the number of requests using this parameter
  </action>
  <verify>
    <automated>grep -n "Count\|Usage" frontend/src/pages/settings/control-parameters.tsx | head -20</automated>
  </verify>
  <done>Usage column renamed to Count in all tabs</done>
</task>

<task type="auto">
  <name>Task 2: Add edit button and edit sheet to control parameters</name>
  <files>frontend/src/pages/settings/control-parameters.tsx</files>
  <action>
    Add edit functionality to control parameters page:
    1. Import Pencil icon from lucide-react
    2. Add edit mutation hook: useUpdateControlParameter from useControlParameters
    3. Add state for tracking which parameter is being edited: const [editingId, setEditingId] = useState&lt;number | null&gt;(null)
    4. Modify ParameterRow to include edit button in the actions cell (before archive/restore)
    5. Create edit Sheet that opens when edit button is clicked
    6. The edit sheet form should:
       - Pre-fill with current values
       - Allow editing name and description fields
       - Have category (for Request Types/Request States), level (for Priorities) fields as disabled/read-only
       - Submit calls update mutation
    7. Each tab (RequestTypeTab, RequestStateTab, PriorityTab, CategoryTab) needs to handle its own edit state and provide appropriate form
  </action>
  <verify>
    <automated>grep -n "Pencil\|edit\|setEditingId" frontend/src/pages/settings/control-parameters.tsx | head -15</automated>
  </verify>
  <done>Edit button appears for each parameter, clicking opens sheet with editable name and description (category/level disabled)</done>
</task>

<task type="auto">
  <name>Task 3: Add update mutation hook to useControlParameters</name>
  <files>frontend/src/hooks/useControlParameters.ts</files>
  <action>
    Add update functionality to the hooks:
    1. Read the existing useControlParameters.ts hook file
    2. Add useUpdateControlParameter hook using useMutation
    3. Call the PATCH endpoint (to be created in backend plan)
    4. Properly type the update function
  </action>
  <verify>
    <automated>grep -n "useUpdateControlParameter" frontend/src/hooks/useControlParameters.ts</automated>
  </verify>
  <done>Update mutation hook available for control parameter editing</done>
</task>

</tasks>

<verification>
- [ ] Usage column renamed to "Count" in any tab
- [ ] Each row has edit button that opens edit sheet
- [ ] Edit sheet has name field as editable (not read-only)
- [ ] Edit sheet has category/level fields as disabled (read-only)
- [ ] Edit sheet has description as editable field
- [ ] Save updates the parameter via API
</verification>

<success_criteria>
Control parameters page displays with "Count" column, edit button works for each parameter, edit sheet allows editing name and description with category/level as read-only.
</success_criteria>

<output>
After completion, create `.planning/phases/11-settings-control-parameters/11-01-SUMMARY.md`
</output>
